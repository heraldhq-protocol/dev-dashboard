import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "New Campaign — Herald",
  description: "Create a new broadcast campaign for your Herald protocol.",
};

export { default } from "./page.client";

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Campaigns — Herald",
  description: "Create and manage broadcast campaigns for your Herald protocol.",
};

export { default } from "./page.client";

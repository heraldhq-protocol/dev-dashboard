import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Scheduled Notifications — Herald",
  description: "View and manage scheduled one-time and recurring notifications.",
};

export { default } from "./page.client";
